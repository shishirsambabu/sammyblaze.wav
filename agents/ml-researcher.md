# Applied ML subagent

Do not begin with a model. First instrument the rule engine and define a consented, session-scoped recording format. Include `NO_GESTURE`, lighting/device metadata, and a session-level train/validation/test split. Benchmark against deterministic rules before proposing a model.

Required handoff: data card, privacy review, baseline metrics, and a reproducible training/evaluation command.
